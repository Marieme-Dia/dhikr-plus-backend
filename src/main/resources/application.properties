# ============================================================
#  Dhikr+ — application.properties (Production Mode)
# ============================================================

# Server
server.port=8080

# PostgreSQL (Neon.tech via Pooler)
# Note : On utilise le host 'ep-sparkling-sun-amu1i911-pooler' que tu as fourni
spring.datasource.url=jdbc:postgresql://ep-sparkling-sun-amu1i911-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_XQ0bu8LjlCZA
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=dhikrPlusSecretKey2024VeryLongSecretKeyForJWTSigningAtLeast256Bits
app.jwt.expiration-ms=86400000

# CORS
app.cors.allowed-origins=https://dhikr-plus-frontend.vercel.app